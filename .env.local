
# HARUS sama dengan IP LAN PC saat ini (cek `ipconfig`, adapter WiFi aktif),
# dan sama dengan IP yang ditambahkan ke CORS_ALLOWED_ORIGINS di gowms/.env.
# IP lama 10.11.12.173 di sini sudah tidak sama dengan IP PC sekarang
# (192.168.1.7, sesuai log `npm run dev`) — makanya request dari HP gagal
# walau frontend-nya sendiri sempat kebuka.
NEXT_PUBLIC_API_BASE_URL=http://10.11.12.173:8080/stockrsd
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080/stockrsd